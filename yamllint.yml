extends: default

ignore: |
  ansible/galaxy_roles
  ansible/galaxy_collections

rules:
  document-start: disable
  truthy: 
    allowed-values:
      - 'true'
      - 'false'
      - 'yes'
      - 'no'
  quoted-strings: disable
  line-length: disable
